# CLAUDE.md

Project rules for Ting. Global rules in `~/.claude/CLAUDE.md` apply on top.

## What this is

Offline-first PWA that keeps track of what a household owns and where it is: one table of things with user-defined properties as columns, fast spec search, encrypted at rest. Plan and design system live in `dev_only/` (gitignored). User documentation is `BRUKERVEILEDNING.md` (Norwegian, klarspråk); `README.md` describes the app, `SECURITY.md` the threat model. A change a user can see updates the guide in the same commit.

## Version

0.1.0 (MAJOR.MINOR.PATCH). Bump here and in `package.json` together.

## Stack

| Layer | Choice |
|---|---|
| UI | React 19, TypeScript strict, Vite |
| Data | Dexie (IndexedDB), Zod at every boundary |
| Offline | vite-plugin-pwa (Workbox), `registerType: 'prompt'` |
| Icons | Self-hosted SVG paths in `src/components/Icons.tsx` |
| Tests | Vitest, pure logic only (`src/**/*.test.ts`) |
| Styling | Plain CSS, tokens in `src/styles/tokens.css` |

No CSS framework, no router library, no search library, no external fonts or CDNs. Production build injects a `default-src 'self'` CSP; `connect-src` adds the two lookup hosts.

Deployed to GitHub Pages at https://elzacka.github.io/ting/ by `.github/workflows/deploy-pages.yml` on every push to `main`; `.github/workflows/ci.yml` runs tests and build on pull requests. `vite.config.ts` uses `base: '/ting/'` for builds only; the dev server stays at `/`. The PWA manifest and service worker are served in dev too (`devOptions.enabled`), so Chrome offers install on localhost.

## Commands

| Command | Does |
|---|---|
| `npm run dev` | Dev server on 5173 |
| `npm run build` | Typecheck then production build to `dist/` |
| `npm run preview` | Serve `dist/` |
| `npm test` | Unit tests |
| `npm run typecheck` | `tsc` only |

## Structure

| Path | Holds |
|---|---|
| `src/db/schema.ts` | Zod schemas and types. Field names are English; the plan's Norwegian names map 1:1 |
| `src/db/db.ts` | Dexie instance, `addItem`, `updateItem`, `deleteItem` |
| `src/db/useSealedQuery.ts` | Subscription hook over Dexie `liveQuery`: watches sealed rows, reads the decrypted view |
| `src/lib/search.ts` | Query parser and matcher: fuzzy words, phrases, negation, `key<value`, `har:` (`has:` reads the same). Pure and tested |
| `src/lib/searchTips.ts` | Søketips: the word examples are fixed, the comparisons are built from the register's own columns and values, so each one finds something; a row whose column does not exist is left out. Pure and tested |
| `src/lib/grid.ts` | Items to table columns and cells and back. Pure and tested |
| `src/lib/export.ts` | CSV (`;`, BOM, comma decimals) and download helper. Tested |
| `src/lib/filter.ts` | `parseNumber`, `distinct`, `recentValues` (a field's values, the one used last first, then the offered ones nothing holds) and `suggest` (what to offer under a field as it is typed). Tested |
| `src/lib/filters.ts` | Filter values with counts per column (a date column facets by year), `applyFilters`, `withoutFilter` (what one menu counts: the rows every other filter and the search leave), and the level ids a place column filters by (`levelId`, `splitLevel`: "<column>#2" is that column's second level, so nothing downstream needs to know the types), and `isFacet`: whether a column gets a menu, judged over the whole register, never the rows in view (a date always; otherwise a dozen values or fewer, or values that repeat; prices and order numbers go to the search). Tested |
| `src/lib/format.ts` | nb-NO numbers and dates, real minus sign, narrow no-break space before units |
| `src/lib/strings.ts` | Every user-facing string. Nothing hardcoded in components |
| `src/lib/route.ts` | Hash router: `#/` and `#/oversikt` (the one main screen; `#/registrer` is an alias), `#/innstillinger`, `#/ting/ny`, `#/ting/:id` |
| `src/lib/backup.ts` | `ting.json` format (format 1). Folder copy keeps photos as files in `bilder/`; download copy embeds them as data URLs. Tested |
| `src/lib/folderStore.ts` | File System Access: pick folder, permissions, read, write, reconcile (newer side wins) |
| `src/lib/useFolderSync.ts` | Keeps the folder in sync after every change, debounced 500 ms. Skips the first emission after reconcile |
| `src/lib/prefs.ts` | Per-device choices in localStorage: the idle-lock setting (default on), the columns hidden from the table (Tilpass visning; ids only, validated on read) and whether long values wrap (default off) |
| `src/lib/useAutoLock.ts` | Locks after 10 minutes without pointer or key input; re-checks when the tab becomes visible. Paused while the table has unsaved edits; can be turned off on Innstillinger |
| `src/lib/errors.ts` | `errorText`: what gets logged about an error (name and message, never the object) |
| `src/lib/categoryIcons.ts` | Which pack icon stands for a category: the one chosen in Endre kategorier (`categoryIconFor`, stored on the Kategori property), else one guessed from the words in the name by the pack's keywords, else the neutral `category`. `isBookCategory`: whether that icon is the book, which is what shows the ISBN lookup. Pure and tested |
| `src/icons/` | The category icon pack: one SVG per icon in `category/`, named by its id, and `pack.ts` with one line per icon (Norwegian name, the keywords that suggest it, `source` when it is not Material Symbols). Only the viewBox and path data are read (`svg.ts`), so an icon is drawn by the app's own `<svg>`, never inserted. `README.md` there says how to add, change or remove one and lists sources and licences; `pack.test.ts` keeps files and lines in step. Tested |
| `src/lib/categories.ts` | `applyCategoryEdit`: what Endre kategorier does to the things and the properties (renames on every thing, every column that belongs to the category and the Kategori list, merge on a shared name, the chosen icons, new categories). Pure and tested; `saveCategories` in `db.ts` seals and writes it in one transaction |
| `src/lib/paths.ts` | A place as one string: `parsePath` (typed with `/`, `>` or `›`), `formatPath` (always written back with `›`), `pathPrefix` (the way in as far as one level), `pathsInUse` (every place and every place on the way to one, for the suggestions), `nextLevels` (the levels to tap next: under a place in use, beside one at its end, or beside the level being typed). Three levels: sted, sone, boks. Pure and tested |
| `src/lib/report.ts` | `groupItems`: the things a printed report covers, split by one column's value, two spellings of one value together, the ones with no value last. Pure and tested |
| `src/lib/summary.ts` | The line above the table: totals per kr property over the rows on screen, so a filtered register says what the filter leaves (elzacka, 22 September 2026); a kr property none of them has a value in gets no total. Pure and tested |
| `src/lib/useRowWindow.ts` | Both tables and the phone list render only the rows on screen (fixed row height, page scroll, padding keeps the height) |
| `src/lib/useNarrow.ts` | True below 600 px, or on a touch screen under 500 px tall (a phone on its side): the phone product. Not a mode, the size decides |
| `src/lib/useKeyboardInset.ts` | How much of the window the on-screen keyboard covers, as `--kb` on the root, from `visualViewport`; the phone's bars sit that far up |
| `src/lib/barcode.ts` | `classify` (isbn, ean, upc by GS1 check digit, else other), `cleanCode`, `decodeImage`: the browser's `BarcodeDetector` first, then `zxing-wasm` (the one image dependency; its wasm ships in `dist/`, precached, loaded on the first scan, never from a CDN; `'wasm-unsafe-eval'` in the CSP is for it). Tested |
| `src/lib/lookup.ts` | The one network call: "Slå opp på nett" sends the digits of an ISBN to Nasjonalbiblioteket and Open Library (the Norwegian catalogue first for group 82, the other as fallback) and returns a name for the user to review. Other codes are only stored: the open catalogue for goods other than food is small, and the large ones need a key the app has no server to keep (elzacka, 24 September 2026). The two hosts are the `connect-src` list in `vite.config.ts`; change both together |
| `src/lib/paste.ts` | `parseBlock`: a spreadsheet block (tabs, newlines) for the table, filling right and down from the cell it lands in; `splitLinks`: http(s) addresses in text, for the detail page. Tested |
| `src/components/` | One file per screen or reusable piece. `Overview` is the main screen, one card. Its head is one line: the category button from the left, lined up with Navn, and at the right edge either the add, CSV and print icons, or while things are ticked the selection bar (n valgt, Endre verdi: one property set to one value on every ticked thing as unsaved edits, Skriv ut and Last ned for the ticked things only, Slett with its confirmation, and a cross that clears the ticks) (add opens a menu: Legg til ting, a new row, or Legg til egenskap (kolonne), the property form). Kategori is the view, not a filter menu: one click changes the rows, the columns and the filters; its icon is the one chosen in Endre kategorier, else one guessed from the words in the name (`lib/categoryIcons.ts`). The categories are one button that opens a dropdown (fixed to the window, arrow keys, Escape and a click outside close it): Alle, then every category with its icon and count and, when chosen, a check mark in front (no boxes: a menu, not a form; staying open is what says several can be chosen, as in GitHub's label picker and Notion) (categories made in Endre kategorier and not yet used are listed at 0), Endre kategorier last, and while a table shows, Fjern alle valgte above them (before that the field itself says Velg kategori). Several categories can be chosen at once and choosing keeps the list open; Alle and Fjern alle valgte close it, and unticking the last is Velg kategori. Endre kategorier opens a panel in the band (`CategoryEditor`): each category's icon, name and count; the icon opens `IconPicker`, the pack as a grid (the chosen one ringed, the name of the one under the pointer or keyboard at the foot, arrows, Enter, Escape, a search once the pack passes 24, Velg ut fra navnet back to the guess); a name renames the category everywhere; Ny kategori adds one; nothing is stored before Lagre. The field names the ticked categories. With several ticked, Kategori is a column again, Bruk bare i in the property form covers all of them, and a new row takes the category of the newest thing among them. The register opens on Velg kategori and no table: the table waits until a category is picked (`viewPicked`, held in `App` so opening a thing and coming back does not send you to the start), and anything that means "show me things" opens it too — a new row, a search, or having fewer than two categories to pick between. Fjern alle valgte hides the table again. The button carries the chosen category's icon and name (elzacka, 23 September 2026). Under the head line the count, the totals and Vis n kolonner til as one small grey line, then a tinted band with whatever is open, one at a time (search and filters, the print form, or the property form: name and type first, and under it, as help and not as controls, the properties every category has and, inside one category, those it has on top; a name another category already uses is taken into this category rather than made twice; Endre egenskaper under it opens `PropertyEditor`: every property but Kategori on one line, name, field type, unit of a number, the categories it belongs to (a checklist, none ticked is every category) and a mark to remove it, stored with one Lagre through `removeProperty`, `renameProperty` and `setPropertyCategories`, removing values only after a confirmation that names them and counts the things; refused while the table has unsaved edits), then the table, whose foot row reads Legg til ting and becomes a new row on click (print asks which columns go on paper, Navn always, starting from the columns on screen like Cmd+P, and two things that change the shape of the paper rather than its contents: Grupper etter, a Valgliste column among those on screen, and Ta med bilder. Neither and the table narrows to the choice and prints itself, as it always has; either one and `PrintReport` prints instead, the things one under the other with their photos, under headings that carry a sum per group and a grand total, the grouping column left out of the blocks since the heading already says it. `groupItems` in `lib/report.ts` is the pure part; photos are awaited with `img.decode()` before `window.print()`, or they snapshot blank), search and filters, the table (a name is a link; any other cell turns its row into inputs; unsaved edits stay in memory until "Lagre", which sticks to the bottom while there is something to save; a new row nothing was typed into is dropped when focus leaves the table). A number column shows nb-NO numbers, right-aligned with its header; a column that belongs everywhere and holds one value on every row on screen is left out like the view's own category, except while rows are being typed or changed; a category's own columns always stay. `Grid` is the table skeleton (widths, header cells with resize, windowed body; the card head is `position: sticky` under the top bar and the header row follows it by a transform on scroll, since the sideways-scrolling wrap would pin a sticky header to itself, and Overview measures the head into `--head-h` and hands Grid its height, so the row moves when the head does; rows are 36 px, a mouse at a desk; the right edge fades while there are columns past it; `wrap`, set on Innstillinger, renders every row and lets values run onto more lines). Columns hidden on Innstillinger under Tilpass visning are left out of `shown` before the relevance rule; Navn cannot be hidden. Columns fit their content by default (`autoWidths` in `columnWidths.ts`, measured on a canvas over every row, capped at 420 px) unless the user has dragged or double-clicked one. `ItemDetail` shows a thing, sets its photos and deletes it (every other field is a column in the table). Below 600 px the overview is `ItemList` instead: a list of hits edge to edge (thumbnail, or the category's icon without a photo; name; the place, then the Valgliste values) and, fixed at the bottom where the thumb is, the search field (no Søketips: they teach keyboard syntax) and a round add button; the top bar has no search there. No columns, totals or reports; `useNarrow` decides. `AddItem` is the phone's way in (`#/ting/ny`): photos (the camera or the photo library, as the phone offers), Navn, the Valgliste and Sti columns the typed Kategori belongs to, then Strekkode (Skann strekkode opens the camera and only reads the picture, never keeps it; Slå opp på nett for an ISBN, shown only in a book category: one whose icon is the book, chosen or guessed, `isBookCategory` in `lib/categoryIcons.ts`, where the line under the field says so until it holds an ISBN; serial numbers and QR codes are scanned and stored, never looked up); Enter moves to the next field and saves on the last; Lagre sticks to the bottom above the keyboard; saved one thing at a time, and the Valgliste values stay for the next thing. `ValuePicker` is a field with the values in use under it as chips to tap (a Valgliste's, the one used last first; a place one level at a time), in `AddItem` and on the detail page; the chips never take the focus, so the keyboard stays. `ItemDetail` shows a thing with a row per property its category has (`appliesTo`, the table's rule) and per property it holds a value in, the place first, each edited in place by a click or tap anywhere on the row (the pencil shows on hover and is out of sight on a touch screen) and stored when left (Enter or blur; Escape drops it; a tapped chip stores at once unless a place has a level further in), sets its photos and deletes it (Slett alone at the foot; its confirmation focuses Avbryt). `ErrorBoundary` wraps `main` |
| `src/styles/` | `tokens.css`, `base.css`, `components.css` |

## Encryption

Everything stored is sealed: AES-256-GCM (WebCrypto) under a random data key (DEK); the DEK is wrapped by a key derived from the passphrase with Argon2id (`@noble/hashes`, m=64 MiB, t=3, p=1). `src/lib/crypto.ts` holds the primitives, `src/lib/vault.ts` the session key (memory only; the app opens locked, `lock()` zeroes the key). Before any passphrase the app runs as a trial (`startTrial`): a fresh DEK in memory that nothing wraps, everything sealed under it as usual, a banner (`TrialBanner`) saying it goes when the app closes, with the way to Innstillinger, where Velg et passord comes first, and how to install where the browser can (`lib/useInstall.ts`; Safari on an iPhone keeps Del behind •••). With a passphrase, `InstallBanner` keeps the install line in Safari and on iPhone and iPad until the app is installed, since those browsers clear a site nobody opens for seven days; `main.tsx` asks for `navigator.storage.persist()` at every start. `setupVault` wraps the trial's own DEK, so what was made stays; a restore during a trial adopts the copy's vault; the next start clears what an unwrapped key sealed (`clearTrialRows`). Rows from before encryption skip the trial and go to the setup screen (`hasPlainRows`). Decided by elzacka, 23 September 2026. Face ID or Touch ID can stand in for the passphrase on one device (`src/lib/passkey.ts`, a checkbox under Lås on Innstillinger, off by default): a passkey on the platform authenticator, user verification required, asked for as `internal` with the `client-device` hint so the browser goes straight to Face ID or Touch ID rather than a chooser, whose PRF output through HKDF wraps a second copy of the DEK in the `passkey` setting (Zod-validated on read, never in a file or the folder). The lock screen asks for it once as it shows and keeps a button and the passphrase; it waits until the record is read, so the passphrase field does not raise a phone's keyboard over the sheet. A record whose `dekId` is not the vault's is deleted (a restore or a folder took over another vault); `unlockWithKey` in `vault.ts` takes a key only when its id matches. Decided by elzacka, 24 September 2026. `src/db/db.ts` seals every row: items as one sealed JSON document plus sealed photo bytes, properties as sealed documents, field settings as a sealed setting. Only ids, the folder handle, `localChangedAt` and the vault (wrapped key, salt, parameters) are in the clear. Files on disk are envelopes (`src/lib/backup.ts`): vault in the clear, document sealed; photos in the folder are `bilder/<id>-1.bin`, numbered from one and flat, so the sweep that deletes what no thing claims any more keeps working (elzacka, 22 September 2026); each is 12-byte nonce + ciphertext. A file sealed on another device opens with the passphrase and its key is adopted, so devices converge on one DEK. Plain (unsealed) files and rows still load; rows are sealed on the first unlock.

Changing anything here needs elzacka's confirmation first (global rule on auth, crypto and access control). Threat model and the OWASP Top 10:2025 mapping: `SECURITY.md`.

## Untrusted input

Everything a user types, pastes, restores from a file or reads from a folder is data, never instructions or markup. The guards, path by path, are the A05 and A10 rows of `SECURITY.md`; keep them when changing those paths. The rule for a future language model is there too.

## Data model

Dexie version 4: tables `items` (`id` only), `settings` (`key`) and `properties` (`id` only). No content indexes: they would leak content. Decrypted shapes: `Item` (id, name, specs, photo, createdAt, updatedAt; rows and files may also carry `category` or `note`, which become the properties Kategori and Notat on load, and `ensureCategoryProperty` creates the Kategori property row, a Valgliste at order -2, on unlock if things carry one and it is missing), `Property` (column definitions: `id` = key+unit, `key`, `unit`, `createdAt`, optional `order`, `type`: text, choice, number, date, path, `options` for choice columns, and optional `categories`, the Kategori values the column belongs to; the Kategori property alone may carry `icons`, category as written to a pack icon id, decided by elzacka 23 September 2026). A date property carries the internal unit marker `dato` and a place property the marker `sti`, so their specs format as dates and as paths; the UI shows the type, never the marker. Changing a column's type to Sti therefore changes its specs' unit, never their values, which is how an existing Valgliste of places converts (elzacka, 22 September 2026). Units are not shown in table headers or filter labels (elzacka, 19 September 2026); they appear on the detail page, in the print report and in CSV. Missing `type` on old rows means text, or date if the marker is set. Column order comes from `columnDefs` in `src/lib/fields.ts`: Navn first, always, then the properties by `order`. Navn's label lives in `settings` under key `fields`. Navn cannot be moved or removed: it is the identity of an item everywhere, and the frozen column when the table scrolls sideways. Kategori is the view, and it decides the columns: a column is rendered when it belongs to at least one category in view (`appliesTo` in `fields.ts`, so a Kjøkken column stays for the Kjøkken things when Interiør is chosen too; no `categories` means it belongs everywhere, and with no category chosen only those do) and then either is one a category in view asks for (`claimedBy`: shown even when empty, since inside a category its own columns are the work list) or holds a value for a row on screen. With one category in view its own column is dropped: it says the same on every row. The toolbar's "Vis n kolonner til" brings every column back, which is also how a value survives a change of category. `withCategory` and `setPropertyCategories` narrow a column from the column menu; a column added inside a category belongs to it. The filter panel offers the same columns and never Kategori itself. The same order and labels apply in the table, filters, print and CSV. `createdAt` and `updatedAt` are kept in the data and in CSV but not shown anywhere in the app or the print report (elzacka, 19 September 2026). A property exists independently of item values; removing one strips the matching spec from every item. Adding non-indexed fields needs no version bump. Changing indexes or renaming fields does: add `db.version(5)` with an `upgrade`, never edit an existing version.

Every mutation in `db.ts` bumps `localChangedAt`; loading from a file or folder does not. That is what `reconcile` compares against the folder's `exportedAt`.

`readItems` keeps opened items in memory keyed by id and the nonces of their sealed parts, so a change re-opens only the rows whose seal changed; the cache empties on lock. The items query watches primary keys, not rows. The folder write skips a photo whose file is newer than the item's `updatedAt`; a restore from a backup file asks for one full photo write. In the table only the row being touched has inputs. A new row inherits the Valgliste and date values of the row above (shop and date down a receipt, location down a shelf), never numbers or text; the first row inherits the Kategori of the newest thing.

A thing carries a list of photos, and the first is the one shown wherever there is room for only one: the table, the phone list, the report block. `ItemDetail` and `AddItem` set them; with more than one, `PhotoStrip` draws the lot and any of them can be made the first. Rows and files written before a thing could carry several hold one `photo` and read as a list of one (`sealedPhotos` in `db.ts`, `storedPhotos` in `backup.ts`); nothing is migrated in place, a row takes the new shape the next time it is written. Decided by elzacka, 22 September 2026.

Photos are stored as `Blob`, never base64. A downloaded copy embeds them as data URLs, encoded and decoded by hand rather than through `FileReader` and `fetch`: `connect-src 'self'` blocks fetching a `data:` URL, which used to fail every restore that carried a photo.

Strekkode is a text property created the first time a code is saved from the phone form (`barcodeProperty` in `fields.ts`); retail codes are stored as digits.

Kategori is a property like any other (a Valgliste, first column by default), not a built-in field; the only built-in is Navn. Its cells offer the values already in the column through `<datalist>`. Decided by elzacka, 21 September 2026.

## Conventions

- Language in code, comments, commits: English. Language in UI: Norwegian (nb-NO), klarspråk, du-form
- Norwegian text: always a capital letter after a colon, also when a fragment follows. Overrides the klarsprak-norsk skill on that point. Decided by elzacka, 19 September 2026
- No emoji anywhere
- Commit format `type: description` (feat, fix, docs, refactor, test, chore)
- Commit only after the change has been run locally and verified by elzacka. Never push
- A change to the data model or its logic holds for every use at once: registering (the table, the phone form), viewing (the table, the thing's page, the phone list), searching and filtering, and choosing for print, report and CSV. One rule, such as which properties belong to a category, gives the same answer on all of them. It also keeps the app distraction-free, clean, intuitive and efficient. Decided by elzacka, 23 September 2026
- Design decisions: `dev_only/designsystem.md`. Follow it. One accent colour, no shadows, no illustrations, 44 px targets, visible labels
- Everything from outside (form input, storage) is validated with Zod before it becomes an `Item`
- The lock is the passphrase: once one exists the app always opens locked, the key lives in memory until lock or reload (elzacka, 19 September 2026). Before one exists the app opens as a trial, with no lock button and no idle lock (elzacka, 23 September 2026)
- Header controls: search, the Lås appen button (padlock, drops the key) and the Innstillinger gear. Icon only with `aria-label`, which the stylesheet also shows as a label under the button (in the register card: to its left, since the card clips at its edge) after 300 ms of hover or on keyboard focus (no `title`: the browser's tooltip was slow and inconsistent); the same style for every register action in the card head (elzacka, 21 September 2026). No toolbar of text buttons. Innstillinger is the exception: its actions are rare and some cannot be undone, so they are text buttons at the right of their section's title, Gjenopprett fra sikkerhetskopi on a line of its own in the danger style, and each choice is a checkbox with its sentence as one label under the title; the line under the folder's title is its state. Hiding the search panel keeps the query and the filters; the search icon stays lit while either narrows the table, never for the category, which is the view. No edit mode: the table edits in place, and nothing is stored before "Lagre". Decided by elzacka, 21 September 2026. The phone screens (Ny ting, the thing's page) store at once instead: on a phone nothing is ever unsaved
- Below 600 px the app is the phone product: add one thing, find a thing, look at a thing. Columns, bulk edits, totals, CSV and print are desk work and are not rendered there. No mobile-mode switch; the size decides, and a phone on its side is still a phone. Decided by elzacka, 21 September 2026. On a phone, Tilpass visning is not rendered, and Lagringsmappe is not rendered on a touch screen whose browser cannot reach a folder; the backup goes through the share sheet where the browser can share the file. Password forms carry a hidden `username` field (`KeychainName`) so password managers file the passphrase under Ting. `index.html` sets `viewport-fit=cover`: the top bar pads by the top safe area (inside `--topbar-h`), `body` by the side ones, and the bottom bars by the bottom one
